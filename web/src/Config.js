export const CONFIG = {
    // Physics
    GRAVITY: 0.8,
    JUMP_FORCE: -18,

    // Game Speed
    INITIAL_SPEED: 7,
    MAX_SPEED: 15,
    SPEED_ACCEL: 0.001,

    // Scoring
    SCORE_MULTIPLIER: 0.01,

    // Obstacles
    INITIAL_OBSTACLE_INTERVAL: 1500,
    MIN_OBSTACLE_INTERVAL: 800,
    INTERVAL_DECAY: 50,

    // Aesthetics
    COLORS: {
        CYAN: '#00f3ff',
        MAGENTA: '#ff00ff',
        PURPLE: '#9d00ff',
        GOLD: '#ff9d00',
        BG_SKY: '#050508',
        BG_GROUND: '#1a1a2e'
    },

    // Collision
    HITBOX_MARGIN: 12
};
